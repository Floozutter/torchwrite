import { lightClamp, lightWrap } from "light";
import { describe, it } from "@jest/globals";

describe(lightClamp.name, () => {
    it.todo("prevents NaN propagation");
});

describe(lightWrap.name, () => {
    it.todo("returns 0 instead of throwing");
});
